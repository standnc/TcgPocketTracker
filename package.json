{
  "name": "ptcgp-mcp-server",
  "version": "1.1.0",
  "description": "Provider-agnostic MCP server for a local Pokémon TCG Pocket catalog and collection",
  "private": true,
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "engines": {
    "node": ">=22 <25"
  },
  "bin": {
    "ptcgp-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "smoke": "npm run build && node dist/scripts/smoke.js",
    "verify": "npm run format && npm run lint && npm run test && npm run smoke",
    "start": "node dist/index.js",
    "sync": "node dist/scripts/sync-catalog.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@tesseract.js-data/eng": "^1.0.0",
    "better-sqlite3": "^11.10.0",
    "sharp": "^0.35.4",
    "tesseract.js": "^6.0.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "eslint": "^10.9.1",
    "prettier": "^3.9.6",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.68.0"
  }
}
